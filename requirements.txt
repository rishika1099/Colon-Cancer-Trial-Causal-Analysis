# Loosened to ranges to resolve cross-package constraints on Python 3.11.
# After a successful install, `pip freeze` is committed to requirements.lock
# for byte-exact reproducibility. requirements.txt remains human-readable.

lifelines>=0.27,<0.28
econml>=0.15,<0.16
dowhy>=0.11,<0.13
scikit-learn>=1.3,<1.5
scikit-survival>=0.22,<0.23
pandas>=2.1,<2.3
numpy>=1.24,<2.0
matplotlib>=3.7,<3.10
seaborn>=0.13,<0.14
statsmodels>=0.14,<0.15
rpy2>=3.5,<3.6
shap>=0.43,<0.44
jupyter>=1.0
